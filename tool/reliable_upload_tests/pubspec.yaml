name: zhidu_upload_test_harness
publish_to: none
environment:
  sdk: '>=3.10.0 <4.0.0'
dependencies:
  ai_reader:
    path: ../..
dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
hooks:
  user_defines:
    sqlite3:
      source: system
      name: sqlite3
      name_windows: winsqlite3
flutter:
  uses-material-design: true
