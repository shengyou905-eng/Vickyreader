# 服务端口
PORT=3000

# 运行环境: development | production
NODE_ENV=production

# JWT 签名密钥（生产环境必须修改！随机生成64位：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"）
JWT_SECRET=change-me-to-a-random-64-char-string
